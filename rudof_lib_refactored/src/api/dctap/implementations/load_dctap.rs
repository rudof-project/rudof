use crate::{Result, Rudof, formats::{InputSpec, DCTapFormat}};

/// Implementation stub for `load_dctap` operation.
pub fn load_dctap(
    rudof: &mut Rudof,
    dctap: &InputSpec,
    dctap_format: Option<&DCTapFormat>,
) -> Result<()> {
    todo!()
}
