use crate::{Rudof, Result, formats::{InputSpec, ShapeMapFormat}};

pub fn load_shapemap(
    rudof: &mut Rudof,
    shapemap: &InputSpec,
    shapemap_format: Option<&ShapeMapFormat>,
    base_nodes: Option<&str>,
    base_shapes: Option<&str>,
) -> Result<()> {
    todo!()
}
