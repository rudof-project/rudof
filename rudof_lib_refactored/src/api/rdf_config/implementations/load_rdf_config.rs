use crate::{
    Rudof, Result,
    formats::{InputSpec, RdfConfigFormat}
};

pub fn load_rdf_config(rudof: &mut Rudof, rdf_config: &InputSpec, rdf_config_format: Option<&RdfConfigFormat>) -> Result<()> {
    todo!()
}
