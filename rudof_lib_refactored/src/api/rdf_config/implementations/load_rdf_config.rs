use crate::{
    Rudof, Result,
    formats::{InputSpec, RdfConfigFormat}
};

pub fn load_rdf_config(rudof: &mut Rudof, rdf_config: &InputSpec, format: Option<&RdfConfigFormat>) -> Result<()> {
    todo!()
}
